## Issue

**环境**：Windows（其他平台待确认）
**问题描述**：传入空白姓名参数，程序依旧输出问候语句。
**复现命令**：`sdt-greet --name " "`
**实际结果**：输出 `Hello, !`，进程以退出码 0 退出。
**期望结果**：校验空白姓名入参，不输出问候语，返回非零退出码，提示参数非法。

## 提交信息

```
fix: 校验空白姓名参数，防止畸形问候输出

问题：传入仅空白字符的name参数时无校验，程序输出 Hello, !，退出码0，非法输入被当作成功执行。
解决方案：对name参数做去空白处理，若结果为空则判定输入非法，跳过问候输出，返回非零退出码。
```

## 评审意见

> 
> Blocking：必须修复该缺陷
> 复现行为：运行`sdt‑greet --name " "`，输出`Hello, !`，退出码为 0。
> 风险：无效输入返回成功状态码，外部调用方误认为调用正常，下游获取畸形问候字符串，引发业务异常。
> 建议动作：增加入参校验，去空白后 name 为空时禁止输出问候，返回非零退出码，输出错误提示。

> 
> Suggestion：建议补充单元测试覆盖空白姓名场景，保障回归。

> 
> Nit：提交信息中可写明复现命令，方便查阅。

今天 20:58

 写一个单元测试用例来覆盖空白姓名场景

 如何增加对空白姓名参数的校验？

 除了单元测试，还可以用什么方法保障回归？

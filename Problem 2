cat >> test_greet.py <<'EOF'

def test_greet_blank_name():
    import subprocess
    result = subprocess.run(
        ["python", "-m", "greetlab.main", "   "],
        capture_output=True
    )
    assert result.returncode == 2
EOF
